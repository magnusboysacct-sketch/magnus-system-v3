"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = SettingsMasterCategoriesPage;
var react_1 = require("react");
var supabase_1 = require("../lib/supabase");
function SettingsMasterCategoriesPage() {
    var _a = (0, react_1.useState)(true), loading = _a[0], setLoading = _a[1];
    var _b = (0, react_1.useState)(null), loadError = _b[0], setLoadError = _b[1];
    var _c = (0, react_1.useState)([]), rows = _c[0], setRows = _c[1];
    var _d = (0, react_1.useState)({}), drafts = _d[0], setDrafts = _d[1];
    var _e = (0, react_1.useState)(false), savingAll = _e[0], setSavingAll = _e[1];
    function load() {
        return __awaiter(this, void 0, void 0, function () {
            var _a, data, error, safe_1, e_1;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        setLoading(true);
                        setLoadError(null);
                        _b.label = 1;
                    case 1:
                        _b.trys.push([1, 3, 4, 5]);
                        return [4 /*yield*/, supabase_1.supabase
                                .from("master_categories")
                                .select("id, name, is_active, scope_of_work")
                                .order("name", { ascending: true })];
                    case 2:
                        _a = _b.sent(), data = _a.data, error = _a.error;
                        if (error)
                            throw error;
                        safe_1 = (data !== null && data !== void 0 ? data : []);
                        setRows(safe_1);
                        // initialize drafts (keep existing dirty edits if already present)
                        setDrafts(function (prev) {
                            var _a, _b;
                            var next = __assign({}, prev);
                            for (var _i = 0, safe_2 = safe_1; _i < safe_2.length; _i++) {
                                var r = safe_2[_i];
                                if (!next[r.id]) {
                                    next[r.id] = {
                                        name: ((_a = r.name) !== null && _a !== void 0 ? _a : "").toString(),
                                        is_active: !!r.is_active,
                                        scope_of_work: ((_b = r.scope_of_work) !== null && _b !== void 0 ? _b : "").toString(),
                                        dirty: false,
                                        saving: false,
                                        error: null,
                                    };
                                }
                            }
                            return next;
                        });
                        return [3 /*break*/, 5];
                    case 3:
                        e_1 = _b.sent();
                        console.error("Failed to load master_categories:", e_1);
                        setLoadError(e_1 instanceof Error ? e_1.message : "Failed to load categories");
                        return [3 /*break*/, 5];
                    case 4:
                        setLoading(false);
                        return [7 /*endfinally*/];
                    case 5: return [2 /*return*/];
                }
            });
        });
    }
    (0, react_1.useEffect)(function () {
        load();
    }, []);
    var dirtyIds = (0, react_1.useMemo)(function () { return Object.entries(drafts).filter(function (_a) {
        var d = _a[1];
        return d.dirty;
    }).map(function (_a) {
        var id = _a[0];
        return id;
    }); }, [drafts]);
    function updateDraft(id, patch) {
        setDrafts(function (prev) {
            var _a;
            var cur = prev[id];
            if (!cur)
                return prev;
            var nextRow = __assign(__assign(__assign({}, cur), patch), { dirty: true, error: null });
            return __assign(__assign({}, prev), (_a = {}, _a[id] = nextRow, _a));
        });
    }
    function saveRow(id) {
        return __awaiter(this, void 0, void 0, function () {
            var d, payload_1, error, e_2;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        d = drafts[id];
                        if (!d)
                            return [2 /*return*/];
                        setDrafts(function (prev) {
                            var _a;
                            return (__assign(__assign({}, prev), (_a = {}, _a[id] = __assign(__assign({}, prev[id]), { saving: true, error: null }), _a)));
                        });
                        _a.label = 1;
                    case 1:
                        _a.trys.push([1, 3, , 4]);
                        payload_1 = {
                            name: d.name.trim(),
                            is_active: d.is_active,
                            scope_of_work: d.scope_of_work.trim(),
                        };
                        return [4 /*yield*/, supabase_1.supabase
                                .from("master_categories")
                                .update(payload_1)
                                .eq("id", id)];
                    case 2:
                        error = (_a.sent()).error;
                        if (error)
                            throw error;
                        // mark clean + refresh local rows view
                        setRows(function (prev) {
                            return prev.map(function (r) { return (r.id === id ? __assign(__assign({}, r), payload_1) : r); });
                        });
                        setDrafts(function (prev) {
                            var _a;
                            return (__assign(__assign({}, prev), (_a = {}, _a[id] = __assign(__assign({}, prev[id]), { saving: false, dirty: false, error: null }), _a)));
                        });
                        return [3 /*break*/, 4];
                    case 3:
                        e_2 = _a.sent();
                        console.error("Save row failed:", e_2);
                        setDrafts(function (prev) {
                            var _a;
                            return (__assign(__assign({}, prev), (_a = {}, _a[id] = __assign(__assign({}, prev[id]), { saving: false, error: e_2 instanceof Error ? e_2.message : "Save failed" }), _a)));
                        });
                        return [3 /*break*/, 4];
                    case 4: return [2 /*return*/];
                }
            });
        });
    }
    function saveAll() {
        return __awaiter(this, void 0, void 0, function () {
            var _i, dirtyIds_1, id;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (dirtyIds.length === 0)
                            return [2 /*return*/];
                        setSavingAll(true);
                        _a.label = 1;
                    case 1:
                        _a.trys.push([1, , 6, 7]);
                        _i = 0, dirtyIds_1 = dirtyIds;
                        _a.label = 2;
                    case 2:
                        if (!(_i < dirtyIds_1.length)) return [3 /*break*/, 5];
                        id = dirtyIds_1[_i];
                        // eslint-disable-next-line no-await-in-loop
                        return [4 /*yield*/, saveRow(id)];
                    case 3:
                        // eslint-disable-next-line no-await-in-loop
                        _a.sent();
                        _a.label = 4;
                    case 4:
                        _i++;
                        return [3 /*break*/, 2];
                    case 5: return [3 /*break*/, 7];
                    case 6:
                        setSavingAll(false);
                        return [7 /*endfinally*/];
                    case 7: return [2 /*return*/];
                }
            });
        });
    }
    return (<div className="p-6 space-y-6">
      {/* Header */}
      <div className="flex items-center justify-between gap-3">
        <div>
          <h1 className="text-xl font-semibold">Master Categories</h1>
          <div className="text-sm text-slate-500">
            Edit category scopes here so nobody has to touch Supabase.
          </div>
        </div>

        <div className="flex gap-2">
          <button onClick={load} className="px-3 py-2 rounded bg-slate-800 text-white" disabled={loading || savingAll}>
            Refresh
          </button>

          <button onClick={saveAll} className="px-3 py-2 rounded bg-blue-600 text-white disabled:opacity-50" disabled={dirtyIds.length === 0 || savingAll || loading}>
            {savingAll ? "Saving…" : "Save All (".concat(dirtyIds.length, ")")}
          </button>
        </div>
      </div>

      {/* Load state */}
      {loading ? (<div className="text-sm text-slate-400">Loading categories…</div>) : loadError ? (<div className="text-sm text-red-400">Error: {loadError}</div>) : null}

      {/* Table */}
      {!loading && !loadError ? (<div className="space-y-3">
          {rows.length === 0 ? (<div className="text-sm text-slate-400">No categories found.</div>) : (rows.map(function (r) {
                var d = drafts[r.id];
                if (!d)
                    return null;
                return (<div key={r.id} className="p-4 border border-slate-700 rounded space-y-3">
                  <div className="flex items-center justify-between gap-3">
                    <div className="flex items-center gap-3">
                      <div className="text-sm text-slate-400">Category</div>
                      <input value={d.name} onChange={function (e) { return updateDraft(r.id, { name: e.target.value }); }} className="px-3 py-2 rounded bg-slate-900 border border-slate-700 text-white w-[320px]" placeholder="Category name"/>
                      <label className="flex items-center gap-2 text-sm text-slate-300">
                        <input type="checkbox" checked={d.is_active} onChange={function (e) {
                        return updateDraft(r.id, { is_active: e.target.checked });
                    }}/>
                        Active
                      </label>
                      {d.dirty ? (<span className="text-xs text-yellow-400">Unsaved</span>) : (<span className="text-xs text-slate-500">Saved</span>)}
                    </div>

                    <button onClick={function () { return saveRow(r.id); }} disabled={!d.dirty || d.saving} className="px-3 py-2 rounded bg-slate-800 text-white disabled:opacity-50">
                      {d.saving ? "Saving…" : "Save"}
                    </button>
                  </div>

                  <div className="space-y-1">
                    <div className="text-xs text-slate-400">Scope of Work</div>
                    <textarea value={d.scope_of_work} onChange={function (e) {
                        return updateDraft(r.id, { scope_of_work: e.target.value });
                    }} className="w-full px-3 py-2 rounded bg-slate-900 border border-slate-700 text-white" rows={4} placeholder="Type the default scope that should auto-fill on BOQ sections…"/>
                    {d.error ? (<div className="text-xs text-red-400">{d.error}</div>) : null}
                  </div>
                </div>);
            }))}
        </div>) : null}
    </div>);
}
